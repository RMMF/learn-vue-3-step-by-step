{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "start": "npx serve && npx json-server db.json -p 3001"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "json-server": "^1.0.0-beta.2",
        "laravel-vite-plugin": "^1.0",
        "vite": "^5.0"
    }
}
